import React from 'react';
import { MapSkeletonContainer } from './styles';

const MapSkeleton: React.FC = () => {
  return <MapSkeletonContainer data-testid="map-skeleton" />;
};

export default MapSkeleton;
